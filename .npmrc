# The maintainer's checkout lives in an iCloud-synced folder: keeping pnpm's
# virtual store in a ".nosync" folder stops iCloud from evicting package files.
# Harmless anywhere else.
virtual-store-dir=.pnpm-store.nosync
